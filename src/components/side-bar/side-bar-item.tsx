import React from 'react';
import type {SideBarItemProps} from './side-bar';

const SideBarItem: React.FC<SideBarItemProps> = ({children}) => {
  return <>{children}</>;
};

export default SideBarItem;
